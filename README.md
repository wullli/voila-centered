# Centered voila default template
Defaul voila template adjusted to be centered to 50% of the viewport-width

# Installation
`pip install git+https://github.com/wullli/voila-centered.git`

# Usage
`voila --template=centered example.ipynb`
